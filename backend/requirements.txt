flask>=3.0
flask-cors>=4.0
pandas>=2.0
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
optuna>=3.4

# Optional — backend falls back to sklearn HistGradientBoosting if these
# aren't installed. Recommended for the demo so the model leaderboard
# names match the UI ("XGBoost", "LightGBM").
xgboost>=2.0
lightgbm>=4.0
